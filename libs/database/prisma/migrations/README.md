# Migrations Directory

This directory contains all database migration files generated by Prisma.

Each migration is stored in a timestamped directory with a `migration.sql` file.

Do not manually edit migration files after they have been applied to production.
