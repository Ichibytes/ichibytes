/**
 * Shared types for frontend and backend consumption
 * These are plain TypeScript types (no decorators) that can be used across the monorepo
 */

// Enums are exported from ./enums

// Pagination, Response types, and ErrorResponse are exported from ./interfaces

// Entity interfaces (Post, Project, Media, User, Tag, ProjectLinks) are exported from ./interfaces
